// Lệnh tạo migrate: migrate create -ext sql -dir $(PATH) -seq $(NAME_TABLE)
// Lệnh tạo database: migrate -path db/migrations -database "postgres://user:pass@localhost:5432/myapp_db?sslmode=disable" up

// Debug
// Mỗi service là một container riêng, chạy ở port riêng
// Mỗi service là một database riêng, chạy ở port riêng

// Cấu hình nginx