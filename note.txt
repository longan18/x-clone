Lệnh tạo migrate: migrate create ext sql dir $(PATH) seq $(NAME_TABLE)
Lệnh tạo database: migrate path db/migrations database "postgres://user:pass@localhost:5432/myapp_db?sslmode=disable" up

Debug
Mỗi service là một container riêng, chạy ở port riêng
Mỗi service là một database riêng, chạy ở port riêng

Cấu hình nginx

[viettran][Clean Architecture là gì  Ưu nhược và cách dùng hợp lý] https://200lab.io/blog/cleanarchitectureuunhuocvacachdunghoply
Example:
		[TRANSPOT]                 	   [BUSINESS]                           [STORAGE]
	API update product    ---->	  UpdateProduct(product)    ---->	  <MONGODB>
																		FindProductById(id)
																		UpdateProduct(product)
															---->	  <MYSQL>
																		FindProductById(id)
																		UpdateProduct(product)

--------------------------------------
type A interface {
	Test() string
}

type B struct {
	v1 string
	v2 string
}

type C struct {
	v3 string
	v4 string
}

func (b B) Test() string{
	return v1 + v2
}

func (c C) Test() string{
	return v3 + v4
}

func main() {
	var a A
	a = B{v1: "123", v2: "456"}
	fmt.Println(a.Test())
}